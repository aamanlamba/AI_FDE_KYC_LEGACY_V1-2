# P11 Requirement Traceability — Capability Classification

Stage: P11 (Production Readiness, Red-Team & Final Engineering Review).
Method: each area was re-verified directly against the running system in this review
(live API calls, direct module calls, or the independent eval harness) — not by
re-reading P1–P10's own self-reports. Evidence cited is reproducible: file paths, test
names, and command output captured in [test_evidence.md](test_evidence.md).

Classification legend: **PASS** (implemented, tested, verified working) / **PARTIAL**
(implemented but with a disclosed, real capability boundary) / **FAIL** (claimed but
not actually verifiable, or a genuine defect found) / **NOT_APPLICABLE**.

| # | Capability area | Classification | Evidence |
|---|---|---|---|
| 1 | Document Intelligence (P1) | **PASS** | `src/document_intelligence/` — provider interface (`DocumentIntelligenceProvider`) + deterministic default (`DeterministicSidecarProvider`); schema validation confirmed live: a provider returning a wrong-shaped object raises `ProviderContractError` (verified directly, not just by unit test); an invalid `document_type` enum value raises a Pydantic `ValidationError` at construction (verified directly). Eval: `document_field_exact_match=0.9762 (82/84)` including the one deliberately-corrupted field. `tests/test_document_intelligence.py`. |
| 2 | Identity Resolution (P2) | **PASS** | `src/identity_resolution/` — 5-state `MatchStatus`, deterministic-before-fuzzy comparison, `NAME_FUZZY_THRESHOLD=0.75` confirmed as a distinct, non-authoritative status (`FUZZY_MATCH` never collapses to `MATCH`). Live-verified: CASE-005's real OCR-corrupted name (`Moharnmad Rehrnan` vs `Mohammed Rahman`) correctly resolves to `CONFLICT` at the case level (pairwise `FUZZY_MATCH`/`CONFLICT` mix, not averaged away). Eval: `match_status_accuracy=1.0 (8/8)`, `conflict_detection_accuracy=1.0 (3/3)`. `tests/test_identity_resolution.py`. |
| 3 | Evidence Validation (P3) | **PASS** | `src/evidence_validation/` — 9 rule-based checks, `NOT_IMPLEMENTED` status honestly disclosed for `CHECKSUM-SIGNATURE` (confirmed present verbatim in every live document validation report, not silently dropped). Malformed-date input verified to fail safely (`check_date_format` returns `FAIL`, never raises). `tests/test_evidence_validation.py`. |
| 4 | Fraud & Anomaly Signals (P4) | **PASS** | `src/fraud_signals/` — `FraudAssessmentStatus` keeps `FRAUD_SIGNAL_PRESENT` distinct from an unreachable, reserved `FRAUD_PROVEN`; CASE-006's tamper marker independently verified to reach `REJECT` via a hard-stop risk factor, live. No LLM import anywhere in `src/` (grep-verified: zero `import`/`from` statements referencing openai/anthropic/langchain, and none in `requirements.txt`) — the only textual hit for "LLM" in `src/fraud_signals/provider.py` is a docstring *prohibiting* LLM-based fraud judgments, confirmed by inspection. `tests/test_fraud_signals.py`. |
| 5 | Evidence Aggregation & Risk/Decision Policy (P5) | **PASS** | `src/decision_policy/` — rule-based (not opaque-weighted) policy; every `RiskFactor` carries its own `triggers_hard_stop`; live-verified that a case's reason codes are exactly `{HARD_STOP|REVIEW}:{category}` per factor's own hard-stop flag, not the case's aggregate outcome (a REJECT case can carry a `REVIEW:` code alongside a `HARD_STOP:` code — CASE-004 does). Zero of 6 real cases had an APPROVE decision with any `FAIL` validation result (checked across all document- and case-level results). Reproducibility re-confirmed independently: `assess_case_risk`/`verify_case` produce byte-identical output across repeated calls under one bound trace context. Eval: `decision_accuracy=1.0 (10/10)`, `FA=0/3, FR=0/3`. `tests/test_decision_policy.py`. |
| 6 | Human-in-the-Loop Review (P6) | **PASS, with one defect found and fixed this stage** | `src/review/` — SQLite-backed, append-only audit log; review-open correctly refused (409) for a non-REVIEW-decision case (CASE-006, live-verified); duplicate review-open is idempotent by case; repeated identical reviewer transitions correctly 409. **Found by this review's concurrency red-team**: `IdempotencyCache`'s original `get()`/`put()` sequence around the mutating transition endpoint was not atomic, so a concurrent retry with the same `Idempotency-Key` could land in the window between another thread's DB commit and its cache write and receive a spurious 409 instead of the cached success (no duplicate side effect ever occurred — audit log stayed single-entry). **Fixed**: `IdempotencyCache.get_or_compute()` now holds one lock across the whole check-compute-store sequence (`src/security/idempotency.py`, `src/app.py:transition_review`). Regression test added (`tests/test_architecture.py::test_concurrent_retries_with_the_same_idempotency_key_never_409`, 8 concurrent identical-key requests, asserts zero errors and exactly one audit entry) and unit tests for the new method (`tests/test_security.py`). Full suite re-run green (222 passed) after the fix. See [test_evidence.md](test_evidence.md) §"P11 fix verification". |
| 7 | AI Evals & Regression (P7) | **PASS** | `eval/` — independent of `tests/`; 11 labeled cases across 10 categories; explicit `sample_size_disclaimer`; FAR/FRR reported as scenario-label agreement, not calibrated population error. Re-run live in this review (both in the working tree and from a clean clone): `RELEASE GATE STATUS: PASS` on all 6 gates both times, byte-for-byte-equivalent metrics. `scripts/run_evaluation.py`, `tests/test_eval_harness.py`. |
| 8 | Security, Privacy & AI Guardrails (P8) | **PASS** | `src/security/` — live-verified: reviewer endpoints reject no-credential and wrong-credential requests (401, not a silent 200); path-traversal/oversized/malformed identifiers rejected at the boundary (400/422) for `../secret`, `/etc/passwd`, a NUL-embedded id, and an 80+-char id; a real name value from a live case response was captured and confirmed absent from the corresponding captured log stream; a forced internal 500 (dependency raising `RuntimeError` with an embedded path) returned only `{"detail":"internal error","correlation_id":...}` — no stack trace, class name, or path leaked to the caller; no `eval(`/`exec(` on untrusted content anywhere in `src/`; no real secret patterns or PEM keys in `src/`/`config/` (the one `X-API-Key` value is documented workshop-only, not a secret). `tests/test_security.py`. |
| 9 | Observability & Auditability (P9) | **PASS** | `src/observability/` — every live request in this review emitted a full nested span tree (`http.request` → `service.verify_case` → `document_intelligence.extract`/`evidence_validation.*`/`fraud_signals.*`/`decision_policy.assess`) sharing one `trace_id`/`correlation_id`, structured JSON, and a `decision_lineage` block with `policy_version` + component versions attached to every case response — reconstructed live from `GET /metrics` and captured log output without reading source. `tests/test_observability.py`. |
| 10 | Production Architecture & Modernization (P10) | **PASS, with disclosed scope boundaries** | `tests/test_architecture.py`'s AST-based import-graph check still finds zero circular imports/layering violations after this stage's fix. Provider-interface pattern holds throughout (verified: a nonconforming provider is rejected, not silently accepted). Dependency posture re-verified this stage: `pip-audit -r requirements.txt` → 0 known vulnerabilities; `ruff check src/ tests/ eval/ scripts/` → clean. Concurrency re-verified beyond P10's original smoke test: 30 concurrent case-verify calls and 8 concurrent identical-idempotency-key transition calls both completed with zero errors after this stage's fix (see area 6). Docker build/container smoke test remain **not executable in this environment** (no Docker/Podman daemon here, true since P0) — reviewed but unexecuted locally; CI (`.github/workflows/ci.yml`) runs `docker build` with real Docker but that pipeline itself has not been executed from within any session to date. |

## Summary

9 of 10 areas verified PASS outright; area 6 (HITL review) surfaced one genuine,
narrow concurrency defect during this review's red-team phase, which was fixed and
re-verified in place (not deferred) — see the residual risk register for what remains
open by design rather than by defect.
